# 配置迁移助手

通过 V1 的“V1 配置迁移源”将系统设置、插件配置、启用状态、账号范围和 Bot 路由迁移到 AWBotNest 2。

- 读取后先显示脱敏预览。
- 执行前自动备份 V2 当前 `config.json`。
- 默认仅填充 V2 空值，不覆盖已有有效配置。
- “启用状态”默认不迁移，避免尚未安装的插件被自动启用。
- 系统连接参数迁移后需要重启 V2。

建议两端位于可信内网或使用 HTTPS。迁移完成后请停用或卸载 V1 迁移源。
